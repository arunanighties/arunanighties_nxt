/**
 * SHIPPING_FEE_PER_ITEM defines the flat delivery rate per item (in Rupees).
 * Edit this number manually to change the shipping fee across the application.
 */
export const SHIPPING_FEE_PER_ITEM = 1;
